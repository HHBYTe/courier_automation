I'm migrating per-courier Power BI datasets from a master xlsx source to
the parquet substrate built by this repo. Pilot (Seur) is done; help me
repoint the remaining datasets the same way.

# Read first
- docs/power_bi.md — full recipe, M snippets, procedure, validation
  done on Seur, gateway/path caveats. Start here.
- docs/architecture.md — the "Parquet substrate" section and the UPS
  date-format sniffer note.
- courier_automation/store/workbook_appender.py — `export_parquet` is
  what produces every monthly parquet file.
- scripts/backfill/backfill_<carrier>_parquet.py — historical
  re-materialisation, one per courier.
- scripts/lookups/extract_seur_lookups.py — one-shot xlsx dump of
  static lookups; mirror this per courier when needed.
- scripts/lookups/derive_seur_lookups.py — re-derives Códigos IC and
  SERVICIOS from parquet. Runs automatically after `ingest seur` via
  the post-ingest hook in courier_automation/cli.py:`_run_derive_hook`.

# Where the pilot landed
- Seur dataset: Datos query repointed at `data/seur/*.parquet`;
  static lookups (Delegaciones, Destino, Origen, ClaveFactura) load
  from `data/seur/other/*.csv`; non-static lookups (Códigos IC,
  SERVICIOS) refresh via the derive script after every ingest.
- Relationships, measures, visuals, thin report .pbix files
  unchanged. Loaded 287,480 rows, 0 errors after deleting the
  auto-generated Changed Type step.

# What's left
Datasets still on xlsx (one PBI dataset per courier):
- Correos Express — fact: `Datos` sheet at
  `Operations - Couriers/05. Correos Express/Análisis Envíos Correos Express V2.xlsx`
- Seitrans — `Operations - Couriers/04. Seitrans/Análisis envíos Seitrans.xlsx` (sheet `Datos`)
- UPS — `Operations - Couriers/07. UPS (UK)/UPS Shippings Report.xlsx` (sheet `Data`)
- Wwex — `Operations - Couriers/11. Wwex (US)/Wwex USA Shippings Report.xlsx` (sheet `Data`)
- Spring — `Operations - Couriers/13. Spring (FR)/Shipment Report.xlsx` (sheet `INVOICES`)

For each, the parquet is already on disk at `data/<carrier>/<YYYY>-<MM>.parquet`
(verified by `scripts/backfill/backfill_all_parquet.py`).

# Per-courier procedure I want help with
1. Inventory each dataset's queries — for the carrier I'm working on,
   tell me which queries exist (fact + lookups) and what each one
   sources from. I'll paste the M from Advanced Editor as you ask.
2. For each query, produce a paste-ready M snippet that repoints to
   parquet (fact) or CSV/derived (lookups), matching the Seur recipe.
3. Identify per-carrier lookup tables that aren't static (derived
   from the fact data). For each, propose either an in-Power-Query
   derivation or a new `scripts/lookups/derive_<carrier>_lookups.py`
   that gets wired into the post-ingest hook automatically (see
   `_run_derive_hook` in courier_automation/cli.py — drop a file named
   `derive_<carrier>_lookups.py` and it runs after every `ingest <carrier>`).
4. Call out anything that won't survive a clean repoint: locale-format
   columns (UPS Version had one), mixed-type columns (UPS Charge
   Description Code), dd/mm/yy display hacks (Spring dates), leading-
   zero codes, formulas in derived columns. Parquet drops those
   display hacks by design.

# Constraints
- Don't change `coerce_<carrier>_dtypes`, `assert_plausible`,
  `assert_schema`, `ParseResult`. Parquet only replaces the *write*
  step. The xlsx code paths stay intact.
- `Operations - Couriers/` is read-only. Never write to a master.
- Don't try to edit .pbix binaries — produce M snippets the operator
  pastes into Power BI Desktop's Advanced Editor.
- The manifest pipeline is shimmed (`_NullRegistry` in cli.py); leave
  it that way. Don't re-enable.
- Default ingest format is `--format both`; the parquet write triggers
  the post-ingest derive hook automatically.

# Working style
Short answers, code over commentary. When I paste M from a query,
respond with the replacement M and the list of Applied Steps to delete
(typically Navigation, Promoted Headers, Changed Type). Validate row
counts after each repoint by reading the parquet with pandas and
comparing to what Power BI reports.

# Open question for me to answer at session start
Which courier do I want to repoint next? (Correos Express / Seitrans /
UPS / Wwex / Spring) — pick smallest first if I'm not sure. Spring is
2,626 rows and shortest history; UPS is the trickiest (250 columns,
the date-sniffer edge case).