pandas==2.2.3
openpyxl==3.1.5
typer==0.15.1
click==8.1.8
pyarrow==18.1.0
xlrd==2.0.1
# Graph storage backend (PR2): MSAL for service-principal auth, httpx
# for HTTP. Optional at runtime — only imported when COURIER_BACKEND=graph.
msal==1.31.1
httpx==0.28.1

